# PDF Coordinate Example

Инструмент для визуализации аннотаций и координат в PDF-документах. Проект позволяет загружать PDF-файлы вместе с JSON-аннотациями и создавать изображения страниц с выделенными областями, а также вставлять текст в определенные поля.

## Возможности

- 📄 Загрузка и обработка PDF-документов
- 🏷️ Визуализация различных типов аннотаций (заголовки, параграфы, рисунки, формулы и др.)
- 📍 Работа с координатами в формате `{страница}.{позиция}`
- 🖼️ Экспорт страниц в формате PNG с выделенными областями
- 🔍 Детальная информация о координатах каждой аннотации
- ✏️ **Вставка текста в PDF по координатам с поддержкой кириллицы**
- 🎯 **Автоматическое определение полей для заполнения**

## Установка

### Требования

- Python 3.8 или выше
- pipenv

### Пошаговая установка

1. **Установите pipenv** (если не установлен):

   ```bash
   pip install pipenv
   ```

2. **Клонируйте репозиторий**:

   ```bash
   git clone <repository-url>
   cd pdf-coordinate-example
   ```

3. **Установите зависимости**:

   ```bash
   pipenv install
   ```

4. **Активируйте виртуальное окружение**:
   ```bash
   pipenv shell
   ```

## Использование

### Подготовка данных

1. **Разместите ваши файлы в папке `data/`**:

   ```
   data/
   ├── Shablon.pdf                    # Ваш PDF шаблон
   └── 0b3106da-a713-47b7-90c6-e665bdae74f3.json  # Аннотации
   ```

2. **Формат JSON-аннотаций**:
   ```json
   [
   	{
   		"label": "PARAGRAPH",
   		"vertices": [
   			{ "x": 1.50875, "y": 1.603666280299011 },
   			{ "x": 1.90625, "y": 1.603666280299011 },
   			{ "x": 1.90625, "y": 1.748616895187793 },
   			{ "x": 1.50875, "y": 1.748616895187793 }
   		]
   	}
   ]
   ```

### Запуск

```bash
pipenv run python main.py
```

Выберите режим работы:

- **1** - Вставить ФИО в поле 9 (правильная позиция)
- **2** - Создать изображения с текстом

### Результаты

После выполнения в папке `out/` появятся файлы:

- `filled_shablon.pdf` - PDF с вставленным текстом
- `page-1-with-text.png` - изображения с выделенными областями

## Вставка текста в PDF

### Основной функционал

Проект автоматически определяет правильные поля для вставки текста:

1. **Анализ аннотаций** - поиск подходящих областей по координатам
2. **Определение поля 9** - автоматический поиск области "NAME AND ADDRESS"
3. **Поддержка кириллицы** - использование UnicodeCIDFont для русского текста
4. **Точное позиционирование** - вставка в центр определенной области

### Пример использования

```python
# Автоматическая вставка в поле 9
pipenv run python main.py
# Выберите режим 1
```

### Продвинутое использование

Для более гибкой настройки используйте специализированные скрипты:

```bash
# Анализ доступных аннотаций
pipenv run python analyze_annotations.py

# Вставка в правильную позицию
pipenv run python correct_position_insertion.py

# Гибкая настройка текста
pipenv run python final_text_insertion.py
```

## Типы аннотаций

Проект поддерживает следующие типы аннотаций:

- **TITLE** - заголовки
- **PARAGRAPH** - параграфы текста (используется для поля 9)
- **FIGURE** - рисунки и изображения
- **EQUATION** - математические формулы
- **REFERENCE** - библиографические ссылки
- **FOOTNOTE** - сноски
- **CAPTION** - подписи к рисункам
- **META** - метаданные

## Формат координат

Координаты хранятся в специальном формате:

- **Целая часть** = номер страницы (начиная с 1)
- **Дробная часть** = позиция на странице (0.0 - 1.0)

Пример: `1.8626` означает позицию 0.8626 на странице 1.

## Алгоритм определения поля 9

1. **Фильтрация по странице** - поиск аннотаций на первой странице
2. **Фильтрация по типу** - выбор только PARAGRAPH аннотаций
3. **Фильтрация по позиции** - поиск в нижней части страницы (y > 0.7)
4. **Сортировка по координате Y** - выбор самой нижней области
5. **Вставка в центр** - размещение текста в центре найденной области

## Настройки

### Изменение разрешения изображений

```python
image = page.to_image(resolution=200)  # Увеличить качество
```

### Настройка шрифта

```python
# Автоматический выбор шрифта с поддержкой кириллицы
try:
    pdfmetrics.registerFont(UnicodeCIDFont('STSong-Light'))
    font_name = 'STSong-Light'
except:
    font_name = 'Helvetica'
```

### Настройка размера и цвета текста

```python
can.setFont(font_name, 12)  # Размер шрифта
can.setFillColorRGB(0, 0, 0)  # Черный цвет
```

## Решение проблем

### Ошибка установки зависимостей

Если возникает ошибка при установке Pillow или других пакетов:

1. **Удалите старый lock-файл**:

   ```bash
   del Pipfile.lock
   ```

2. **Переустановите зависимости**:
   ```bash
   pipenv install
   ```

### Ошибка кодировки кириллицы

Если русский текст отображается неправильно:

- Убедитесь, что используется UnicodeCIDFont
- Проверьте, что текст передается в UTF-8 кодировке

### Неправильное позиционирование текста

Если текст вставляется не в том месте:

1. **Запустите анализ аннотаций**:

   ```bash
   pipenv run python analyze_annotations.py
   ```

2. **Проверьте координаты** - поле 9 должно быть в нижней части страницы

3. **Используйте специализированный скрипт**:
   ```bash
   pipenv run python correct_position_insertion.py
   ```

## Примеры использования

### Заполнение страхового сертификата

```bash
# Вставка ФИО в поле 9 страхового сертификата
pipenv run python main.py
# Выберите режим 1
```

### Анализ структуры документа

```bash
# Просмотр всех доступных аннотаций
pipenv run python analyze_annotations.py
```

### Создание изображений с разметкой

```bash
# Создание PNG с выделенными областями
pipenv run python main.py
# Выберите режим 2
```

## Возможные улучшения

1. **Цветовая схема** - разные цвета для разных типов аннотаций
2. **Экспорт координат** - сохранение в CSV формат
3. **Веб-интерфейс** - интерактивный просмотр результатов
4. **Пакетная обработка** - обработка нескольких документов
5. **Настройка стилей** - толщина линий, цвета, прозрачность
6. **Поддержка других полей** - автоматическое заполнение других разделов

## Зависимости

- `pdfplumber` - работа с PDF-документами
- `PyPDF2` - манипуляции с PDF
- `reportlab` - создание PDF с текстом
- `Pillow` - обработка изображений
- `pdfminer.six` - парсинг PDF

## Лицензия

Этот проект предназначен для образовательных целей и демонстрации работы с координатами в PDF-документах.
